[tool.poetry]
name = "catalan-learner"
version = "0.1.0"
description = "Local Catalan flashcard app"
authors = ["Liam Laird"]
[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
